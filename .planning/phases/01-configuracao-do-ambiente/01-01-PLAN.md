---
phase: 01-configuracao-do-ambiente
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "npm run build completes with exit code 0"
    - "npm run dev serves the page at localhost:3000 without console errors"
    - ".env.local contains real Supabase credentials (SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY)"
  artifacts:
    - path: ".env.local"
      provides: "Supabase credentials for server-side API route"
      contains: "SUPABASE_URL"
  key_links:
    - from: "src/app/api/leads/route.ts"
      to: ".env.local"
      via: "process.env.SUPABASE_URL and process.env.SUPABASE_SERVICE_ROLE_KEY"
      pattern: "process\\.env\\.SUPABASE"
---

<objective>
Verify that the local development environment builds and runs without errors.

Purpose: ENV-01 is already complete (.env.local configured). This plan validates BUILD-01 (production build passes) and DEV-01 (dev server serves correctly), fixing any issues found.
Output: A working local environment — `npm run build` exits 0 and `npm run dev` serves on localhost:3000.
</objective>

<execution_context>
@C:\Users\Joao Ranauro\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Joao Ranauro\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@src/app/layout.tsx
@src/app/page.tsx
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify production build (BUILD-01)</name>
  <files></files>
  <action>
    Run `npm run build` in the project root.

    If the build succeeds (exit code 0): Task is complete, no files to modify.

    If the build fails: Diagnose the error from the build output and apply the minimal fix. Common issues to check:
    - TypeScript type errors — fix the specific type issue in the reported file
    - Missing environment variables — verify .env.local has SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY
    - Import resolution errors — check that referenced modules exist
    - ESLint errors blocking build — fix the specific lint issue or adjust next.config.ts if it's a false positive

    DO NOT refactor or change design/copy. Only fix what blocks the build.
    DO NOT change working code. Scope fixes to the exact error reported.

    After any fix, re-run `npm run build` to confirm exit code 0.
  </action>
  <verify>Run `npm run build` and confirm it exits with code 0. The terminal should show "Build completed successfully" or equivalent Next.js success output with no error lines.</verify>
  <done>npm run build completes with exit code 0. All pages compile, no TypeScript or ESLint errors.</done>
</task>

<task type="auto">
  <name>Task 2: Verify dev server (DEV-01)</name>
  <files></files>
  <action>
    Run `npm run dev` to start the development server.

    Verify:
    1. The server starts and reports "Ready" on localhost:3000
    2. Use `curl http://localhost:3000` (or equivalent) to confirm the page returns HTML with status 200
    3. Check that the response contains expected markers: the page title ("use.moema" or similar), the main sections structure

    If the dev server fails to start: Diagnose from the error output and fix minimally, same principles as Task 1.

    After verification, stop the dev server (Ctrl+C or kill the process).

    DO NOT leave the dev server running in the background after verification.
  </action>
  <verify>Start `npm run dev`, confirm "Ready" message appears. Run `curl -s http://localhost:3000 | head -50` and confirm HTML response with 200 status. Stop the server after verification.</verify>
  <done>Dev server starts on localhost:3000, responds with the landing page HTML, no startup errors.</done>
</task>

</tasks>

<verification>
Phase 1 is complete when all three requirements are met:
1. ENV-01: .env.local exists with SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY (already done)
2. BUILD-01: `npm run build` exits with code 0
3. DEV-01: `npm run dev` serves on localhost:3000 and responds with HTML
</verification>

<success_criteria>
- `npm run build` exits 0 with no errors
- `npm run dev` starts successfully and serves the page at localhost:3000
- No code changes required (ideal), or only minimal fixes to unblock build/dev
</success_criteria>

<output>
After completion, create `.planning/phases/01-configuracao-do-ambiente/01-01-SUMMARY.md`
</output>
