---
phase: 03-verificacao-do-pipeline-de-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Git remote 'origin' points to github.com/joaoranauro1/use-moema.git"
    - "GitHub repo joaoranauro1/use-moema exists and is accessible"
    - "A push to master triggers a Vercel deployment"
    - "Production site usemoema.com.br responds with HTTP 200 after deploy"
    - "Production site content is intact after deploy verification"
  artifacts: []
  key_links:
    - from: "local git repo"
      to: "github.com/joaoranauro1/use-moema"
      via: "git remote origin"
      pattern: "github\\.com.joaoranauro1.use-moema"
    - from: "github.com/joaoranauro1/use-moema"
      to: "usemoema.com.br"
      via: "Vercel auto-deploy on push to master"
      pattern: "vercel.*deployment"
---

<objective>
Verify that the existing deploy pipeline is working: local git repo pushes to GitHub, which triggers automatic Vercel deployment to usemoema.com.br.

Purpose: Confirm DEPLOY-01 (Vercel connected to repo) and DEPLOY-02 (push triggers auto-deploy) -- completing the final phase of the local restoration project.
Output: Verified end-to-end pipeline: git push -> GitHub -> Vercel -> usemoema.com.br.
</objective>

<execution_context>
@C:\Users\Joao Ranauro\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Joao Ranauro\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify GitHub connection and recent deploy history</name>
  <files></files>
  <action>
    This is a verification-only task. Do NOT modify any files.

    1. Confirm git remote configuration:
       - Run `git remote -v` and verify origin points to `https://github.com/joaoranauro1/use-moema.git`
       - Run `git branch -vv` to confirm master tracks origin/master

    2. Verify GitHub repo is accessible:
       - Run `gh repo view joaoranauro1/use-moema --json name,url,defaultBranchRef` to confirm the repo exists and default branch is master
       - Run `gh api repos/joaoranauro1/use-moema/deployments --jq '.[0:5] | .[] | {environment, created_at, description}'` to check for recent Vercel deployment records (Vercel creates GitHub deployments when it deploys)

    3. Check local vs remote sync:
       - Run `git fetch origin` to update remote refs
       - Run `git log --oneline origin/master -5` to see latest commits on remote
       - Run `git status` to confirm local master is up to date with origin/master (or note any divergence)

    4. Verify production site is currently live:
       - Run `curl -s -o /dev/null -w "%{http_code}" https://usemoema.com.br` to confirm HTTP 200
       - Run `curl -s https://usemoema.com.br | head -50` to confirm it returns the Next.js landing page HTML (look for lang="pt-BR" and section content)

    If GitHub deployments API returns empty (Vercel might use its own system instead of GitHub Deployments API), that is acceptable -- the key evidence is in Task 2 where we test the actual push-to-deploy flow.
  </action>
  <verify>
    - git remote origin matches https://github.com/joaoranauro1/use-moema.git
    - gh repo view succeeds (repo exists and is accessible)
    - git fetch succeeds (can reach remote)
    - curl to usemoema.com.br returns HTTP 200
  </verify>
  <done>DEPLOY-01 confirmed: Git remote points to correct GitHub repo, repo is accessible, production site is live</done>
</task>

<task type="auto">
  <name>Task 2: Test push-to-deploy pipeline with a trivial commit</name>
  <files>README.md</files>
  <action>
    Test the end-to-end deploy pipeline by making a harmless commit and pushing it.

    1. Record the current state of production for comparison:
       - Run `curl -s -o /dev/null -w "%{http_code}" https://usemoema.com.br` (should be 200)
       - Note the current time (for comparing with deploy timestamp later)

    2. Make a trivial, non-breaking change:
       - Read README.md first
       - Append a blank line or update the "last verified" timestamp at the bottom of README.md
       - Example: Add a line `<!-- deploy-pipeline-verified: YYYY-MM-DD -->` at the very end
       - This ensures the commit is harmless and doesn't affect the site

    3. Commit and push:
       - `git add README.md`
       - `git commit -m "chore: verify deploy pipeline (Phase 3)"`
       - `git push origin master`
       - Confirm push succeeds with exit code 0

    4. Wait for Vercel to deploy (typically 30-90 seconds for this project):
       - Wait 90 seconds to allow Vercel to pick up the push and deploy
       - Then check `gh api repos/joaoranauro1/use-moema/deployments --jq '.[0] | {environment, created_at, state, description}'` to see if a new deployment was created after the push
       - Also check `gh api repos/joaoranauro1/use-moema/commits/master/status --jq '.statuses[0:3] | .[] | {context, state, target_url}'` for Vercel deployment status checks

    5. Verify production site still works after deploy:
       - Run `curl -s -o /dev/null -w "%{http_code}" https://usemoema.com.br` (must be 200)
       - Run `curl -s https://usemoema.com.br | grep -c 'lang="pt-BR"'` (must be >= 1)
       - Run `curl -s https://usemoema.com.br | grep -o 'id="[a-z]*"' | sort` to confirm all section anchors are still present

    NOTE: If the GitHub Deployments API or commit status API does not show Vercel data (some Vercel configurations use their own webhook system), the key proof is: (a) git push succeeded, and (b) production site responds with 200 and correct content after the push. The human checkpoint in Task 3 will confirm the Vercel dashboard shows the deployment.
  </action>
  <verify>
    - git push origin master exits with code 0
    - After waiting, curl to usemoema.com.br returns HTTP 200
    - Production site HTML still contains lang="pt-BR" and section anchors
    - gh api shows a deployment record newer than the push (or push succeeded and site is live)
  </verify>
  <done>DEPLOY-02 confirmed: Push to master succeeded, production site is live and serving correct content after deploy</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Verified the full deploy pipeline:
    1. Git remote correctly points to joaoranauro1/use-moema on GitHub
    2. Pushed a trivial commit to master
    3. Confirmed production site (usemoema.com.br) responds correctly after push

    The automated checks confirm the pipeline works from the CLI side. Human verification is needed to confirm the Vercel dashboard shows the deployment.
  </what-built>
  <how-to-verify>
    1. Go to https://vercel.com/dashboard (or your Vercel team dashboard)
    2. Find the "use-moema" project
    3. Confirm it shows:
       - Connected to GitHub repo: joaoranauro1/use-moema
       - Branch: master
       - A recent deployment triggered by the "chore: verify deploy pipeline" commit
       - Deployment status: Ready/Success (green)
    4. Visit https://usemoema.com.br in your browser
    5. Confirm the site loads correctly -- scroll through all sections to verify nothing broke
    6. (Optional) Check that the Vercel deployment log shows a successful build
  </how-to-verify>
  <resume-signal>Type "approved" if Vercel dashboard confirms the auto-deploy pipeline is working, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- DEPLOY-01: Git remote origin -> GitHub repo joaoranauro1/use-moema confirmed accessible
- DEPLOY-02: Push to master triggers Vercel deploy; production site live and correct after push
- Production site usemoema.com.br serves HTTP 200 with all sections intact
</verification>

<success_criteria>
1. Vercel dashboard shows connection to repo joaoranauro1/use-moema on branch master
2. A test push confirms auto-deploy is triggered and completes successfully
3. Production site usemoema.com.br continues working correctly after the deploy verification
</success_criteria>

<output>
After completion, create `.planning/phases/03-verificacao-do-pipeline-de-deploy/03-01-SUMMARY.md`
</output>
