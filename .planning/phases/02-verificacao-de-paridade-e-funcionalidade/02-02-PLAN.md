---
phase: 02-verificacao-de-paridade-e-funcionalidade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true

must_haves:
  truths:
    - "A POST to /api/leads with valid InterestModal payload returns 200 and success: true"
    - "The submitted test lead appears in the Supabase leads table"
    - "Invalid payloads are rejected with appropriate error codes (422)"
  artifacts:
    - path: "src/app/api/leads/route.ts"
      provides: "POST endpoint for lead capture"
      exports: ["POST"]
    - path: "src/lib/leads.ts"
      provides: "Zod validation schemas and submitLead client helper"
      exports: ["leadSchema", "interestLeadSchema", "contactLeadSchema", "submitLead"]
    - path: "src/lib/supabase.ts"
      provides: "Supabase client for server-side operations"
  key_links:
    - from: "src/components/ui/InterestModal.tsx"
      to: "/api/leads"
      via: "submitLead() in src/lib/leads.ts calls fetch('/api/leads')"
      pattern: "fetch.*api/leads"
    - from: "src/app/api/leads/route.ts"
      to: "supabase.from('leads').insert"
      via: "Supabase client insert"
      pattern: "supabase\\.from.*leads.*insert"
---

<objective>
Test the InterestModal lead submission flow end-to-end: send a valid lead payload to the /api/leads endpoint and verify it reaches the Supabase database.

Purpose: Confirm LEADS-01 -- the lead capture pipeline (InterestModal -> /api/leads -> Supabase) works correctly in the local environment with real Supabase credentials.
Output: Verified lead submission with database confirmation.
</objective>

<execution_context>
@C:\Users\Joao Ranauro\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Joao Ranauro\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/api/leads/route.ts
@src/lib/leads.ts
@src/lib/supabase.ts
@src/components/ui/InterestModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test lead API endpoint with valid and invalid payloads</name>
  <files></files>
  <action>
    1. Ensure the dev server is running on localhost:3000 (or 3001). Start it if not already running.
    2. Send a valid InterestModal lead payload via curl:
       ```
       curl -s -X POST http://localhost:3000/api/leads \
         -H "Content-Type: application/json" \
         -d '{
           "source": "modal_interesse",
           "name": "Teste Verificacao",
           "email": "teste.verificacao@example.com",
           "phone": "(11) 99999-0000",
           "motivation": ["investimento"],
           "budget": "500k-1m"
         }'
       ```
       Expected: HTTP 200, body contains `{"success":true}`

    3. Send an invalid payload (missing required fields) to verify validation:
       ```
       curl -s -X POST http://localhost:3000/api/leads \
         -H "Content-Type: application/json" \
         -d '{"source": "modal_interesse", "name": "Test"}'
       ```
       Expected: HTTP 422, body contains error about invalid data

    4. Send a malformed JSON to verify error handling:
       ```
       curl -s -X POST http://localhost:3000/api/leads \
         -H "Content-Type: application/json" \
         -d 'not json'
       ```
       Expected: HTTP 400, body contains error about invalid payload

    Do NOT modify any files. This is verification only.
    IMPORTANT: Use a clearly fake test email (teste.verificacao@example.com) so it can be identified and cleaned up.
  </action>
  <verify>
    - Valid payload returns HTTP 200 with {"success":true}
    - Invalid payload returns HTTP 422
    - Malformed JSON returns HTTP 400
  </verify>
  <done>API endpoint correctly accepts valid leads and rejects invalid ones with proper error codes</done>
</task>

<task type="auto">
  <name>Task 2: Verify test lead exists in Supabase database</name>
  <files></files>
  <action>
    1. Query the Supabase leads table to confirm the test lead from Task 1 was inserted.
       Use the Supabase MCP if available, or use curl with the Supabase REST API:
       ```
       # Read SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY from .env.local
       # Then query:
       curl -s "$SUPABASE_URL/rest/v1/leads?email=eq.teste.verificacao@example.com&select=*" \
         -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
         -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
       ```

    2. Verify the returned row contains:
       - source: "modal_interesse"
       - name: "Teste Verificacao"
       - email: "teste.verificacao@example.com"
       - phone: "(11) 99999-0000"
       - motivation: ["investimento"]
       - budget: "R$ 500 mil a R$ 1 milhao" (the readable label, mapped by the API route)

    3. Clean up: Delete the test lead from Supabase to avoid polluting the production table:
       ```
       curl -s -X DELETE "$SUPABASE_URL/rest/v1/leads?email=eq.teste.verificacao@example.com" \
         -H "apikey: $SUPABASE_SERVICE_ROLE_KEY" \
         -H "Authorization: Bearer $SUPABASE_SERVICE_ROLE_KEY"
       ```

    4. Confirm deletion by querying again -- should return empty array.

    Do NOT modify any source files. Only interact with the database for verification and cleanup.
  </action>
  <verify>
    - Supabase query returns the test lead with correct field values
    - Test lead is successfully deleted after verification
    - Final query confirms empty result (clean state)
  </verify>
  <done>Lead submission pipeline verified end-to-end: InterestModal payload -> /api/leads -> Supabase insert confirmed and test data cleaned up</done>
</task>

</tasks>

<verification>
- LEADS-01: Valid lead payload submitted via curl returns 200 and appears in Supabase leads table
- Validation works: invalid payloads rejected with 422, malformed JSON with 400
- Test data cleaned up from Supabase after verification
</verification>

<success_criteria>
1. POST /api/leads with valid InterestModal data returns {"success":true}
2. The lead row exists in Supabase with correctly mapped fields
3. Invalid/malformed requests are properly rejected
4. Test lead cleaned up from database
</success_criteria>

<output>
After completion, create `.planning/phases/02-verificacao-de-paridade-e-funcionalidade/02-02-SUMMARY.md`
</output>
